/**
 * Once I join, will serve as an option type which can act as base 
 * type which canbe used in select drop down. 
 */
export interface Option {
    id: number,
    label: string,
    value: string,
}